if (get_application_name() == "firefox") then
    set_window_workspace(1)
end

if (get_application_name() == "spotify-launcher") then
    set_window_workspace(2)
end

if (get_application_name() == "discord" or get_application_name() == "telegram-desktop") then
    set_window_workspace(3)
end

